---
- name: Testing
  hosts: cumulus
  gather_facts: false

  handlers:
    - name: SaveConfig
      become: true
      ansible.builtin.command:
        nv config apply -y

  tasks:
    - name: Applying command
      become: true
      ansible.builtin.shell: "{{ item }}"
      with_items:
        - nv unset system
        - nv unset service
        - nv unset router
        - nv unset vrf
        - nv unset evpn
        - nv unset nve
        - nv unset interface lo
        - nv unset interface swp1-6
        - nv unset bridge domain br_default
        - nv unset interface vlan1-4094
      notify: SaveConfig
